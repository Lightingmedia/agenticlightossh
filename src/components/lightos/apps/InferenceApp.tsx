// Inference app: developer-facing inference console (Cerebras-style).
// Reuses the Token Factory UI so developers can mint tokens & stream from LightOS models.
export { TokenFactoryApp as InferenceApp } from "./TokenFactoryApp";
